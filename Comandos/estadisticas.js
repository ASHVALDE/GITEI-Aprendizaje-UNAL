const index = require("../GITEI.js")
const {List, Buttons } = require('whatsapp-web.js');

module.exports = {
    run: async (message, usuario, data) => {
        
        message.reply("❌ Caracteristica no implementada - Bot en desarrollo pues xd")
        
    }
}